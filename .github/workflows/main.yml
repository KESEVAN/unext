name: test
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build a project
        run: echo "Successful project"
      - name: Deploy a project
        run: echo "Deploy"
